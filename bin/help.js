// Generated by CoffeeScript 1.9.3
(function() {
  var help;

  module.exports = help = {
    checkType: function(variable, type) {
      if (/[a-z]/.test(type.charAt(0))) {
        type = type.charAt(0).toUpperCase() + type.substr(1);
      }
      return ("[object " + type + "]") === Object.prototype.toString.call(variable);
    },
    isFloat: function(num) {
      if (!help.checkType(num, 'number')) {
        return false;
      }
      return Math.round(num) !== num;
    },
    wait: function(time, fn) {
      return setTimeout(fn, time);
    }
  };

}).call(this);
